# =============================================================================
# ClubOS Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values

# -----------------------------------------------------------------------------
# Supabase Configuration
# -----------------------------------------------------------------------------
PUBLIC_SUPABASE_URL=http://localhost:54321
PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-anon-key

# Service role key - ONLY for server-side operations (migrations, seeding)
# Never expose this in client-side code!
SUPABASE_SECRET_KEY=your-service-role-key

# Alias (some tools expect this name)
SUPABASE_URL=http://localhost:54321

# -----------------------------------------------------------------------------
# Stripe Configuration
# -----------------------------------------------------------------------------
# Get your keys from https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your-stripe-secret-key

# -----------------------------------------------------------------------------
# Demo Seed Credentials (used by scripts/seed.ts)
# -----------------------------------------------------------------------------
# After running `bun db:seed`, use these credentials to log in:
#
# Role     | Email                | Password
# ---------|----------------------|----------
# owner    | owner@clubos.app     | Owner123!
# admin    | admin@clubos.app     | Admin123!
# manager  | manager@clubos.app   | Manager123!
# staff    | staff@clubos.app     | Staff123!
#
# These are hardcoded in the seed script for simplicity.
# In production, use proper user registration flow.
